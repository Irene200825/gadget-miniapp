<view class="weui-cells" wx:if="{{historyList.length>0}}">
  <view class="weui-cell" wx:for="{{historyList}}" wx:key="item._id">
     {{item.time}}
  </view>
</view>

<view class="no-data" wx:if="{{noData}}">没有更多啦~~~</view>